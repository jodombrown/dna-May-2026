/**
 * FeedCommunityPulse — now renders the redesigned DNA Right Rail.
 * (Pulse Compass, DIA Daily Brief, Trending in DNA, Ask DIA CTA)
 */

import React from 'react';
import { DnaRightRail } from '@/components/right-rail/DnaRightRail';

export const FeedCommunityPulse: React.FC = () => <DnaRightRail />;

export default FeedCommunityPulse;
