## Phase B · B2 — Pre-drop gate: PASSED

Pre-drop audit results:

| chk | detail | value |
|---|---|---|
| row_count | feed_bookmarks | 0 |
| row_count | feed_comments | 0 |
| row_count | feed_reactions | 0 |

Zero `live_consumer` rows (no functions, views, or triggers reference `feed_reactions`, `feed_comments`, or `feed_bookmarks`).

Gate satisfied: all three row counts are 0, no live consumers. Safe to proceed with the drop.

## Step 3 — Tracked migration: drop the three dead tables

Create ONE tracked Supabase migration with exactly this SQL (no CASCADE, per your spec — RLS policies drop with the table; any dependency failure means STOP):

```sql
DROP TABLE IF EXISTS public.feed_reactions;
DROP TABLE IF EXISTS public.feed_comments;
DROP TABLE IF EXISTS public.feed_bookmarks;
```

Migration description will note: 0-row tables, no CASCADE, dependency failure = stop signal.

## Step 4 — Post-drop re-audit

After the migration lands in `schema_migrations`, run the post-drop gate SQL you provided and report the four results (pre-drop already above; migration filename; post-drop gate; types.ts regen confirmation).

`types.ts` will be regenerated by Lovable automatically after migration approval; the `feed_reactions` / `feed_comments` / `feed_bookmarks` entries will drop out.

## Not in scope

- B1 and B3 stay queued as parallel tracks
- No app/code changes (no consumers exist)
- No archival (0-row tables)
